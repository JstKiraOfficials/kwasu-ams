/**
 * @file index.ts
 * @module components/layout
 *
 * Barrel export for the application layout components. Import from this path
 * to keep import statements decoupled from the internal file structure.
 *
 * @example
 * ```ts
 * import { AppShell, Sidebar, TopNav } from '@/components/layout';
 * ```
 */

export { AppShell } from './AppShell';
export { Sidebar } from './Sidebar';
export { TopNav } from './TopNav';
